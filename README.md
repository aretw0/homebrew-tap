# @aretw0 Homebrew Tap

Este é o repositório oficial de fórmulas [Homebrew](https://brew.sh/) para as ferramentas e utilitários desenvolvidos por [@aretw0](https://github.com/aretw0).

## 🚀 Como Usar

### 1. Adicionar o Tap

Para registrar este repositório em sua instalação local do Homebrew, execute:

```bash
brew tap aretw0/tap
```

### 2. Instalar uma Ferramenta

Uma vez adicionado o tap, você pode instalar qualquer uma das ferramentas disponíveis:

```bash
brew install <formula>
```

*Nota: Substitua `<formula>` pelo nome da ferramenta desejada.*

### Instalação Direta

Se preferir não adicionar o tap permanentemente, você pode instalar diretamente referenciando o caminho completo:

```bash
brew install aretw0/tap/<formula>
```

## 🔍 Descobrindo Ferramentas

Como este repositório está em constante evolução, você pode listar todas as fórmulas atualmente disponíveis usando o comando de busca do Homebrew:

```bash
brew search aretw0/tap
```

## 📂 Estrutura do Repositório

Todos os manifestos de instalação (Ruby formulae) estão localizados no diretório [`/Formula`](./Formula).

## ⚖️ Licença

Os termos de licenciamento podem variar para cada ferramenta. Por favor, consulte o manifesto individual da fórmula ou o repositório de origem da ferramenta para detalhes específicos.

---
*Mantido com ❤️ por [@aretw0](https://github.com/aretw0)*
